import React from 'react';

const Header = (props)=>{
    return (
        <div>
            <h1 className="App-title">HackerFeed</h1>
            <h2 className="App-desc">Your own HackerNews feed</h2>
        </div>
    );
};

export default Header;